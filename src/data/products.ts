/**
 * @deprecated Import from `@/lib/data` instead.
 * Re-exports kept for backward compatibility with existing pages.
 */
export {
  getFeaturedProducts,
  getProductById,
  getProductBySlug,
  getProductsByCategory,
  products,
  searchProducts,
} from "@/lib/data";
