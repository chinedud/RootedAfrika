/**
 * @deprecated Import from `@/types` instead.
 * Re-exports kept for backward compatibility with existing components.
 */
export type {
  CartItem,
  CartLine,
  Product,
  ProductCategory,
} from "@/types";
export { CATEGORY_LABELS } from "@/types";
